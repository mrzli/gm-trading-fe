import React from 'react';

export interface CompletedTradeItemProps {
}

export function CompletedTradeItem({}: CompletedTradeItemProps): React.ReactElement {
  return (<div>{'CompletedTradeItem'}</div>)
}
